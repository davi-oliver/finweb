"use client";

/** Placeholder: POST /api/personal-finance/transactions com account_id, category_id, kind, amount. */
export function PfTransactionForm() {
  return (
    <p className="text-sm text-zinc-500">
      Formulário de lançamento — use <code className="rounded bg-zinc-100 px-1 dark:bg-zinc-900">POST /api/personal-finance/transactions</code>.
    </p>
  );
}
