"use client";

/** Placeholder: alimentar com série temporal de `/api/personal-finance/summary` ou agregações customizadas. */
export function PfResultOverTimeChart() {
  return (
    <div className="rounded-xl border border-dashed border-zinc-300 p-8 text-center text-sm text-zinc-500 dark:border-zinc-700">
      Gráfico de evolução (resultado no tempo) — conecte a um chart library e ao endpoint de resumo.
    </div>
  );
}
