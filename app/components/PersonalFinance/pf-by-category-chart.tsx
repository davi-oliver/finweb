"use client";

/** Placeholder: usar `expense_by_category_id` de `/api/personal-finance/summary`. */
export function PfByCategoryChart() {
  return (
    <div className="rounded-xl border border-dashed border-zinc-300 p-8 text-center text-sm text-zinc-500 dark:border-zinc-700">
      Distribuição por categoria — mapear IDs para nomes com `/api/personal-finance/categories`.
    </div>
  );
}
